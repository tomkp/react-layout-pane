{
  "name": "react-layout-pane",
  "description": "React flexbox layout component",
  "main": "src/Layout.js",
  "version": "0.0.2",
  "repository": {
    "type": "git",
    "url": "https://github.com/tomkp/react-layout"
  },
  "author": "tomkp <tom@tomkp.com>",
  "keywords": [
    "react",
    "react-component",
    "flexbox",
    "es6"
  ],
  "dependencies": {
    "react": "^0.13.0-rc1"
  },
  "devDependencies": {
    "babel-core": "^4.6.6",
    "babel-jest": "^4.0.0",
    "babelify": "^5.0.4",
    "browserify": "^9.0.3",
    "jest-cli": "^0.4.0",
    "react-tools": "^0.13.0-rc1"
  },
  "scripts": {
    "test": "jest",
    "build": "browserify src/Layout.js -t -o dist/Layout.js",
    "demo": "browserify demo/Example.js -t -o demo/bundle.js"
  },
  "jest": {
    "rootDir": "./src",
    "scriptPreprocessor": "../preprocessor.js",
    "unmockedModulePathPatterns": [
      "react"
    ]
  },
  "browserify": {
    "transform": [
      [
        "babelify"
      ]
    ]
  }
}
