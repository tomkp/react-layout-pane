{
  "name": "react-layout-pane",
  "description": "React flexbox layout component",
  "main": "src/Layout.js",
  "version": "0.0.2",
  "repository": {
    "type": "git",
    "url": "https://github.com/tomkp/react-layout"
  },
  "author": "tomkp <tom@tomkp.com>",
  "keywords": [
    "react",
    "react-component",
    "flexbox"
  ],
  "devDependencies": {
    "babel-core": "^4.6.6",
    "babel-jest": "^4.0.0",
    "babelify": "^5.0.4",
    "browserify": "^9.0.3",
    "jest-cli": "^0.4.0"
  },
  "scripts": {
    "test": "jest",
    "demo": "browserify demo/Demo.js -t babelify --outfile demo/Bundle.js",
    "build": "browserify src/Layout.js -t babelify --outfile dist/Layout.js"
  },
  "dependencies": {
    "react": "^0.13.0-rc1"
  },
  "jest": {
    "rootDir": "./src",
    "scriptPreprocessor": "../preprocessor.js",
    "unmockedModulePathPatterns": [
      "react"
    ]
  }
}
